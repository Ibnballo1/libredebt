{
  "name": "libredebt-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter libredebt-web dev",
    "dev:mobile": "pnpm --filter libredebt-mobile start",
    "build:web": "pnpm --filter libredebt-web build",
    "db:push": "pnpm --filter libredebt-web db:push"
  }
}
